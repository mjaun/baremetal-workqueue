#pragma once

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdlib.h>

typedef bool bool_t;         ///< boolean

typedef uint32_t u32_ms_t;   ///< time in milliseconds (32 bit unsigned)
typedef int32_t i32_ms_t;    ///< time in milliseconds (32 bit signed)
typedef uint64_t u64_ms_t;   ///< time in milliseconds (64 bit unsigned)
typedef int64_t i64_ms_t;    ///< time in milliseconds (64 bit signed)
typedef uint32_t u32_us_t;   ///< time in microseconds (32 bit unsigned)
typedef int32_t i32_us_t;    ///< time in microseconds (32 bit signed)
typedef uint64_t u64_us_t;   ///< time in microseconds (64 bit unsigned)
typedef int64_t i64_us_t;    ///< time in microseconds (64 bit signed)
